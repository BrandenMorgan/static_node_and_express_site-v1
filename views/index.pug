extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
      
      h1 Let's realize your vision, together!
      
      p.lead.text-light
        | Welcome to my portfolio! A simple static website built with the JavaScript express web framework and pug templates.  Each project demonstrates a different programming concept ranging from Object Oriented JavaScript to form validation and working with APIs. 
        
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      
      each project in data.projects     
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`${project.image_urls[0]}`)
            h5= project.project_name
      
